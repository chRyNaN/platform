apply plugin: 'konan'
apply plugin: 'maven'

konan.targets = ['iphone', 'iphone_sim']

konanArtifacts {
    library('kotlinplatform') {
        enableMultiplatform true
    }
}

dependencies {
    expectedBy project(':common')
}